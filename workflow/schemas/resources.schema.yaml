$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
      mem_mb:
        type: integer
        description: default memory used for a rule
      mem_per_cpu:
        type: integer
        description: default memory used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluster for a rule

  required:
    - default_resources

  add_mosdepth_coverage_to_gvcf:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'add_mosdepth_coverage_to_gvcf' rule
      time:
        type: string
        description: max execution time for the 'add_mosdepth_coverage_to_gvcf' rule
      mem_mb:
        type: integer
        description: memory used for 'add_mosdepth_coverage_to_gvcf'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'add_mosdepth_coverage_to_gvcf'
      partition:
        type: string
        description: partition to use on the cluster for 'add_mosdepth_coverage_to_gvcf'

  add_multi_snv_in_codon:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'add_multi_snv_in_codon'
      time:
        type: string
        description: max execution time for 'add_multi_snv_in_codon'
      mem_mb:
        type: integer
        description: memory used for 'add_multi_snv_in_codon'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'add_multi_snv_in_codon'
      partition:
        type: string
        description: partition to use on the cluster for 'add_multi_snv_in_codon'

  artifact_annotation:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'artifact_annotation'
      time:
        type: string
        description: max execution time for 'artifact_annotation'
      mem_mb:
        type: integer
        description: memory used for 'artifact_annotation'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'artifact_annotation'
      partition:
        type: string
        description: partition to use on the cluster for 'artifact_annotation'

  background_annotation:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'background_annotation'
      time:
        type: string
        description: max execution time for 'background_annotation'
      mem_mb:
        type: integer
        description: memory used for 'background_annotation'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'background_annotation'
      partition:
        type: string
        description: partition to use on the cluster for 'background_annotation'

  bgzip_vcf:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'bgzip_vcf'
      time:
        type: string
        description: max execution time for 'bgzip_vcf'
      mem_mb:
        type: integer
        description: memory used for 'bgzip_vcf'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'bgzip_vcf'
      partition:
        type: string
        description: partition to use on the cluster for 'bgzip_vcf'

  calculate_seqrun_background:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'calculate_seqrun_background' rule
      time:
        type: string
        description: max execution time for the 'calculate_seqrun_background' rule
      mem_mb:
        type: integer
        description: memory used for 'calculate_seqrun_background'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'calculate_seqrun_background'
      partition:
        type: string
        description: partition to use on the cluster for 'calculate_seqrun_background'

  hotspot_annotation:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'hotspot_annotation' rule
      time:
        type: string
        description: max execution time for the 'hotspot_annotation' rule
      mem_mb:
        type: integer
        description: memory used for 'hotspot_annotation'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'hotspot_annotation'
      partition:
        type: string
        description: partition to use on the cluster for 'hotspot_annotation'

  simple_sv_annotation:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'simple_sv_annotation' rule
      time:
        type: string
        description: max execution time for the 'simple_sv_annotation' rule
      mem_mb:
        type: integer
        description: memory used for 'simple_sv_annotation'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'simple_sv_annotation'
      partition:
        type: string
        description: partition to use on the cluster for 'simple_sv_annotation'

  snpeff:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'snpeff' rule
      time:
        type: string
        description: max execution time for the 'snpeff' rule
      mem_mb:
        type: integer
        description: memory used for 'snpeff'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'snpeff'
      partition:
        type: string
        description: partition to use on the cluster for 'snpeff'

  sort_vcf:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'sort_vcf'
      time:
        type: string
        description: max execution time for 'sort_vcf'
      mem_mb:
        type: integer
        description: memory used for 'sort_vcf'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'sort_vcf'
      partition:
        type: string
        description: partition to use on the cluster for 'sort_vcf'

  tabix_vcf:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'tabix_vcf'
      time:
        type: string
        description: max execution time for 'tabix_vcf'
      mem_mb:
        type: integer
        description: memory used for 'tabix_vcf'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'tabix_vcf'
      partition:
        type: string
        description: partition to use on the cluster for 'tabix_vcf'

  vep:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'vep' rule
      time:
        type: string
        description: max execution time for the 'vep' rule
      mem_mb:
        type: integer
        description: memory used for 'vep'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'vep'
      partition:
        type: string
        description: partition to use on the cluster for 'vep'
