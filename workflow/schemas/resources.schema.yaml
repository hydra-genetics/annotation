$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
      mem_mb:
        type: integer
        description: default memory used for a rule
      mem_per_cpu:
        type: integer
        description: default memory used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluster for a rule

  required:
    - default_resources

  simple_sv_annotation:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'simple_sv_annotation' rule
      time:
        type: string
        description: max execution time for the 'simple_sv_annotation' rule
      mem_mb:
        type: integer
        description: memory used for 'simple_sv_annotation'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'simple_sv_annotation'
      partition:
        type: string
        description: partition to use on the cluster for 'simple_sv_annotation'

  snpeff:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'snpeff' rule
      time:
        type: string
        description: max execution time for the 'snpeff' rule
      mem_mb:
        type: integer
        description: memory used for 'snpeff'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'snpeff'
      partition:
        type: string
        description: partition to use on the cluster for 'snpeff'

  vep:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'vep' rule
      time:
        type: string
        description: max execution time for the 'vep' rule
      mem_mb:
        type: integer
        description: memory used for 'vep'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'vep'
      partition:
        type: string
        description: partition to use on the cluster for 'vep'

  calculate_seqrun_background:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by the 'calculate_seqrun_background' rule
      time:
        type: string
        description: max execution time for the 'calculate_seqrun_background' rule
      mem_mb:
        type: integer
        description: memory used for 'calculate_seqrun_background'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'calculate_seqrun_background'
      partition:
        type: string
        description: partition to use on the cluster for 'calculate_seqrun_background'
